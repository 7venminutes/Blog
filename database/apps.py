"""
when I create an app 'database' in django project, django gave me this.
don't know what it is......
"""
from django.apps import AppConfig


class DatabaseConfig(AppConfig):
    """
    when I create an app 'database' in django project, django gave me this.
    don't know what it is......
    """
    name = 'database'
